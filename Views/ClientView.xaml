<UserControl x:Class="organizerFitness.Views.ClientView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:organizerFitness.Views" xmlns:fa="http://schemas.fontawesome.io/icons/"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition />
        </Grid.RowDefinitions>

        <TextBox x:Name="txtb_search" HorizontalAlignment="Right" Width="120" Margin="0,0,40,0"></TextBox>
        <Button HorizontalAlignment="Right" Grid.Row="0" Height="25" Width="25" Margin="0,0,5,0" BorderBrush="Transparent" BorderThickness="0" Background="Transparent">
            <fa:FontAwesome Icon="Search" FontSize="20" />
        </Button>


        <DataGrid x:Name="grdClients" Grid.Row="1" ItemsSource="{Binding }" />
    </Grid>
</UserControl>
