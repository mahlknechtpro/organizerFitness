<Window x:Class="organizerFitness.forms.clientTrainingValues"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:organizerFitness.forms"
        mc:Ignorable="d"
        Title="Training Values" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>

        <Button Height="20" Width="75" Grid.Column="0" Grid.Row="1" HorizontalAlignment="Right" Margin="0,0,100,0" Content="Save" Click="SaveSettings_Click"></Button>
        <Button Height="20" Width="75" Grid.Column="0" Grid.Row="1" HorizontalAlignment="Right" Margin="0,0,10,0" Content="Cancel" Click="ReturnSettings_Click"></Button>
    </Grid>
</Window>
